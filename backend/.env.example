# Database Configuration
# You can use either individual variables or DATABASE_URL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hospital-system
DB_USER=postgres
DB_PASSWORD=postgres
# Or use DATABASE_URL instead:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hospital-system

# JWT Configuration
JWT_SECRET=YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER=HospitalSystem
JWT_AUDIENCE=HospitalSystemUsers
JWT_EXPIRE_MINUTES=60

# Kavenegar SMS Service
KAVENEGAR_API_KEY=your_kavenegar_api_key_here
KAVENEGAR_TEMPLATE=verify
KAVENEGAR_TYPE=sms

# Redis (optional)
REDIS_CONNECTION_STRING=localhost:6379

# CORS (optional, comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ASP.NET Core Environment
# ASPNETCORE_ENVIRONMENT=Development

# Port (optional, defaults to 5000)
# PORT=5000
