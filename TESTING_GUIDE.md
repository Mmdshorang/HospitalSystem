# 🚀 راهنمای اجرا و تست سیستم بیمارستان

## 📋 پیش‌نیازها

1. **.NET 8 SDK** نصب باشد
2. **Node.js** (نسخه 18 یا بالاتر) نصب باشد
3. **PostgreSQL** نصب و در حال اجرا باشد

---

## 🔧 راه‌اندازی Backend

### 1. تنظیم Connection String

فایل `appsettings.json` در `backend/HospitalSystem.Api/` را باز کنید و connection string دیتابیس را تنظیم کنید:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=HospitalDB;Username=postgres;Password=YOUR_PASSWORD"
  }
}
```

**نکته:** `YOUR_PASSWORD` را با رمز عبور PostgreSQL خود جایگزین کنید.

### 2. اجرای Migration

```bash
cd backend/HospitalSystem.Api
dotnet ef database update
```

این دستور دیتابیس را ایجاد می‌کند و تمام جداول را می‌سازد.

### 3. اجرای Backend

```bash
dotnet run
```

Backend روی `http://localhost:5255` اجرا می‌شود.

**Swagger UI:** برای مشاهده و تست API ها به `http://localhost:5255/swagger` بروید.

---

## 🎨 راه‌اندازی Frontend

در یک terminal جدید:

```bash
cd frontend
npm install
npm run dev
```

Frontend روی `http://localhost:5173` اجرا می‌شود.

---

## 🧪 تست کامل Authentication

### مرحله 1: ثبت‌نام (Register)

1. به `http://localhost:5173/register` بروید
2. فرم را با اطلاعات زیر پر کنید:

**کاربر Admin نمونه:**
```
نام: علی
نام خانوادگی: احمدی
ایمیل: admin@test.com
رمز عبور: Admin@123
تأیید رمز عبور: Admin@123
```

**کاربر معمولی نمونه:**
```
نام: سارا
نام خانوادگی: محمدی
ایمیل: sara@test.com
رمز عبور: Test@123
تأیید رمز عبور: Test@123
شماره تلفن: 09123456789
کد ملی: 1234567890
جنسیت: زن
تاریخ تولد: 2000-01-01
```

3. روی "ثبت‌نام" کلیک کنید
4. اگر موفق باشد، به صفحه داشبورد redirect می‌شوید

### مرحله 2: خروج (Logout)

1. در sidebar، روی آیکون کاربر یا دکمه "خروج" کلیک کنید
2. به صفحه Login redirect می‌شوید

### مرحله 3: ورود (Login)

1. به `http://localhost:5173/login` بروید
2. با اطلاعات ثبت شده وارد شوید:

```
ایمیل: admin@test.com
رمز عبور: Admin@123
```

3. روی "ورود" کلیک کنید
4. به داشبورد وارد می‌شوید

---

## 📊 تست API های پزشکان (Providers)

### پیش‌نیاز: ایجاد تخصص (Specialty)

قبل از اضافه کردن پزشک، باید حداقل یک تخصص وجود داشته باشد.

#### روش 1: از Swagger

1. به `http://localhost:5255/swagger` بروید
2. به endpoint `POST /api/specialties` بروید
3. روی "Try it out" کلیک کنید
4. در قسمت Authorization، Token را اضافه کنید:
   - از browser console: `localStorage.getItem('authToken')`
   - Token را کپی کنید
   - در Swagger: `Bearer YOUR_TOKEN`
5. Body را با این مقدار پر کنید:

```json
{
  "categoryId": null,
  "name": "قلب و عروق",
  "description": "متخصص قلب و عروق"
}
```

6. "Execute" را بزنید

#### روش 2: از Frontend (توسط Admin)

فعلاً صفحه مدیریت تخصص‌ها وجود ندارد، از Swagger استفاده کنید.

### اضافه کردن پزشک

1. وارد سیستم شوید
2. به صفحه "پزشکان" (`/doctors`) بروید
3. روی "افزودن پزشک" کلیک کنید
4. فرم را پر کنید:

**نکته مهم:** ابتدا باید یک User در سیستم ثبت شود، سپس ProviderProfile به آن UserId متصل می‌شود.

**گام 1: ثبت User جدید**
- از صفحه Register یک کاربر جدید با role "doctor" ثبت کنید
- یا از Swagger به endpoint `POST /api/auth/register` بروید و role را "doctor" قرار دهید

**گام 2: اضافه کردن Provider Profile**
- در فرم "افزودن پزشک":
  - شناسه کاربر (UserId): شناسه user ای که ساختید (از دیتابیس یا response register)
  - شناسه تخصص: ID تخصصی که ساختید (از response specialty)
  - درجه: MD
  - سال‌های تجربه: 5
  - فعال: ✓

5. "ذخیره" کنید

### تست جستجو و فیلتر

1. در صفحه پزشکان، در search box تایپ کنید (نام، ایمیل، تخصص)
2. از dropdown تخصص، یک تخصص را انتخاب کنید
3. لیست به صورت real-time فیلتر می‌شود

---

## 🔍 بررسی مشکلات (Troubleshooting)

### مشکل 1: Backend اجرا نمی‌شود

**خطا:** `Could not connect to database`
- مطمئن شوید PostgreSQL در حال اجرا است
- Connection string را در `appsettings.json` بررسی کنید
- دیتابیس را manually ایجاد کنید: `CREATE DATABASE HospitalDB;`

**خطا:** `Build failed`
- Package version conflict ها وجود دارد
- سعی کنید: `dotnet clean` و سپس `dotnet build`

### مشکل 2: Frontend به Backend وصل نمی‌شود

**خطا:** `Network Error` یا `CORS Error`
- مطمئن شوید Backend روی port 5255 اجرا شده
- CORS در Backend تنظیم شده (در `Program.cs` بررسی کنید)
- در `.env` فایل frontend، `VITE_API_BASE_URL=http://localhost:5255` تنظیم شده

### مشکل 3: بعد از Login، صفحه بارگذاری می‌شود اما چیزی نمایش نمی‌دهد

- AuthContext bug داشت که اصلاح شد
- Cache browser را پاک کنید
- LocalStorage را پاک کنید: `localStorage.clear()`

### مشکل 4: نمی‌توانم پزشک اضافه کنم

**خطا:** "شناسه کاربر (UserId) الزامی است"
- ابتدا باید یک User در سیستم ثبت کنید
- از Register یک کاربر جدید بسازید
- UserId را از دیتابیس یا response API بگیرید

**خطا:** "خطا در افزودن پزشک"
- مطمئن شوید User با آن ID وجود دارد
- مطمئن شوید Specialty با آن ID وجود دارد
- از Swagger log های بیشتری بگیرید

---

## 📝 داده‌های نمونه برای تست

### تخصص‌های پیشنهادی:

```json
[
  {"name": "قلب و عروق", "description": "متخصص قلب و عروق"},
  {"name": "مغز و اعصاب", "description": "متخصص مغز و اعصاب"},
  {"name": "ارتوپدی", "description": "متخصص استخوان و مفاصل"},
  {"name": "چشم پزشکی", "description": "متخصص چشم"},
  {"name": "کودکان", "description": "متخصص اطفال"}
]
```

از Swagger این تخصص‌ها را یکی یکی اضافه کنید.

### کاربران نمونه:

**Admin:**
```
Email: admin@hospital.com
Password: Admin@123456
Role: admin
```

**پزشک 1:**
```
Email: doctor1@hospital.com
Password: Doctor@123
FirstName: دکتر احمد
LastName: محمدی
Role: doctor
```

**پزشک 2:**
```
Email: doctor2@hospital.com
Password: Doctor@123
FirstName: دکتر مریم
LastName: حسینی
Role: doctor
```

**بیمار:**
```
Email: patient@test.com
Password: Patient@123
FirstName: علی
LastName: رضایی
Role: patient
```

---

## 🎯 چک لیست تست

- [ ] Backend اجرا شد بدون خطا
- [ ] Frontend اجرا شد بدون خطا
- [ ] ثبت‌نام کاربر جدید موفق بود
- [ ] ورود به سیستم موفق بود
- [ ] بعد از Login به داشبورد redirect شد
- [ ] خروج از سیستم کار می‌کند
- [ ] بعد از Logout، دسترسی به صفحات protected قطع شد
- [ ] API تخصص‌ها کار می‌کند (GET, POST)
- [ ] API پزشکان کار می‌کند (GET, POST, DELETE)
- [ ] جستجوی پزشکان کار می‌کند
- [ ] فیلتر بر اساس تخصص کار می‌کند
- [ ] حذف پزشک کار می‌کند

---

## 🚀 مراحل بعدی

بعد از تست موفق authentication و Provider API، می‌توانید به مراحل بعدی بروید:

1. **Phase 2:** ساخت API های Clinics، Services، Insurances
2. **Phase 3:** ساخت سیستم درخواست خدمات (ServiceRequest workflow)
3. **Phase 4:** پرداخت و نتایج
4. **Phase 5:** گزارش‌گیری و dashboard

---

## 💡 نکات مهم

1. **Token Expiration:** Token ها بعد از مدتی منقضی می‌شوند. اگر 401 گرفتید، دوباره login کنید.

2. **Role-Based Access:** فقط admin می‌تواند پزشک اضافه/حذف کند. برای تست، حتماً با یک admin login کنید.

3. **Data Relationships:**
   - هر Provider باید به یک User متصل باشه
   - هر Provider می‌تونه به یک Specialty متصل باشه
   - هر Provider می‌تونه به یک Clinic متصل باشه

4. **Database Seeding:** فعلاً database seeding نداریم. باید manual از Swagger یا frontend داده اضافه کنید.

---

## 📞 مشکل دارید؟

اگر با مشکلی روبرو شدید:

1. Browser Console را بررسی کنید (F12)
2. Backend logs را بررسی کنید
3. Swagger را برای تست مستقیم API استفاده کنید
4. PostgreSQL connection را بررسی کنید

---

**موفق باشید! 🎉**
